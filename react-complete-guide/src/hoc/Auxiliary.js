import React from 'react';

//the folder name is 'hoc' coz its mean Higgher Order compornent

const aux = props => props.children;
//children are refer contents between <Aux></Aux>
//which is We used in person.js

export default aux;